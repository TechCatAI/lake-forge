bundle:
  name: lake-forge

resources:
  apps:
    app:
      name: lake-forge
      source_code_path: .
      description: "Metadata-driven ingestion UI"

targets:
  dev:
    default: true
    workspace:
      host: https://adb-1676947424761045.5.azuredatabricks.net  

sync:
  exclude:
    - "**/node_modules/**"
    - "**/__pycache__/**"
