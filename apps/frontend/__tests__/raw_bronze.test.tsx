import { describe, it, expect } from 'vitest'

// simple sanity test to ensure test runner works

describe('raw & bronze pages', () => {
  it('runs sample test', () => {
    expect(1 + 1).toBe(2)
  })
})
